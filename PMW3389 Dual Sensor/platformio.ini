[env:teensy40]
platform = teensy
board = teensy40
framework = arduino

; Use command-line uploader (recommended on macOS)
upload_protocol = teensy-cli

; Optional: explicitly set the upload port if needed
; (run `ls /dev/cu.*` in Terminal to see available ports)
; upload_port = /dev/cu.usbmodemXXXX
